# apogee\_camera\_controller

Control software for the Apogee U9000 CCD camera.

## Overview

The main class for this controller is `camera.py`. It wraps functions  
described in `ApogeeCam.h` into managable sub-routines.
